1. Якщо токен надано:
1.1. Перевірити чи є такий токен в бд
1.2. Переадресовати на такий самий запит без /auth з токеном в хедері

2. Якщо токен не надано:
2.1. Переадресовати на такий самий запит але без /auth
